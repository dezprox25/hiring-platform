# Loaded automatically by Vite in `npm run dev`.
# Must match the port where Nest listens (dezprox-backend `.env` → `PORT`, often 4001 locally).
# If you run the API only via Docker Compose (published port 4000), set this to http://localhost:4000 instead.
VITE_DEV_API_PROXY=http://localhost:4001
